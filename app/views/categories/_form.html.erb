

<%= bootstrap_form_for(category, html: {class: "form-horizontal"}) do |f| %>
  <%= f.text_field :name, placeholder: "example" %>
  <%= f.text_area :description %>
  <%= f.submit "Save" %>
<% end %>
